# class_calendar
A single-file HTML class schedule (timetable) viewer and editor for Chinese university semester courses. Runs entirely in the browser with no backend — all data is persisted in ​localStorage​.（AIGC）
